<% (['development', 'production'] + [@environment]).uniq.flatten.each do |env| %>
<%= env %>:
  browshot:
    api_key: <%= @secrets[:browshot] %>
  slack:
    api_token: <%= @secrets[:slack] %>

<% end %>
